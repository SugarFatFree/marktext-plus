# MarkText Plus 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 使用 Flutter 重写 MarkText Markdown 编辑器，实现多语言、轻量化、配置持久化、双栏编辑模式。

**Architecture:** 四层架构（UI → State(Riverpod) → Service → Platform）。自研 Markdown 解析器和渲染器，JSON 文件配置持久化，flutter_localizations + intl 国际化。三种编辑模式共享同一文本数据源。

**Tech Stack:** Flutter 3.x, Dart 3.x, flutter_riverpod, intl, window_manager, file_picker, path_provider, printing

---

## 文件结构

```
code/
├── pubspec.yaml
├── analysis_options.yaml
├── lib/
│   ├── main.dart                              # 应用入口，初始化配置和 ProviderScope
│   ├── app.dart                               # MaterialApp，路由，主题，国际化绑定
│   ├── core/
│   │   ├── constants.dart                     # 应用常量
│   │   ├── config/
│   │   │   ├── app_config.dart                # 配置数据模型 + toJson/fromJson
│   │   │   └── config_service.dart            # JSON 文件读写服务
│   │   ├── i18n/
│   │   │   ├── l10n/
│   │   │   │   ├── app_en.arb                 # 英文翻译（模板）
│   │   │   │   ├── app_zh.arb                 # 中文翻译
│   │   │   │   ├── app_ja.arb                 # 日文翻译
│   │   │   │   ├── app_ko.arb                 # 韩文翻译
│   │   │   │   ├── app_de.arb                 # 德文翻译
│   │   │   │   ├── app_fr.arb                 # 法文翻译
│   │   │   │   ├── app_it.arb                 # 意大利文翻译
│   │   │   │   ├── app_ru.arb                 # 俄文翻译
│   │   │   │   ├── app_es.arb                 # 西班牙文翻译
│   │   │   │   ├── app_pt.arb                 # 葡萄牙文翻译
│   │   │   │   ├── app_ar.arb                 # 阿拉伯文翻译
│   │   │   │   └── app_pt_BR.arb              # 巴西葡萄牙文翻译
│   │   │   └── l10n.yaml                      # intl 生成配置
│   │   └── theme/
│   │       └── app_theme.dart                 # 5 种主题定义
│   ├── models/
│   │   ├── file_node.dart                     # 文件树节点模型
│   │   └── tab_info.dart                      # 标签页信息模型
│   ├── services/
│   │   ├── file_service.dart                  # 文件读写操作
│   │   └── markdown_parser.dart               # 自研 Markdown 解析器
│   ├── providers/
│   │   ├── editor_provider.dart               # 编辑器状态
│   │   ├── tab_provider.dart                  # 标签页管理
│   │   ├── settings_provider.dart             # 设置状态 + 持久化
│   │   ├── locale_provider.dart               # 语言状态
│   │   └── file_provider.dart                 # 文件树状态
│   ├── ui/
│   │   ├── screens/
│   │   │   ├── home_screen.dart               # 主页面布局
│   │   │   └── settings_screen.dart           # 设置页面
│   │   ├── widgets/
│   │   │   ├── app_menu_bar.dart              # 菜单栏
│   │   │   ├── side_bar.dart                  # 侧边栏（图标列 + 内容区）
│   │   │   ├── editor_tab_bar.dart            # 标签栏
│   │   │   └── status_bar.dart                # 状态栏
│   │   └── editor/
│   │       ├── source_editor.dart             # 源代码编辑器
│   │       ├── markdown_renderer.dart         # Markdown 渲染视图
│   │       ├── split_editor.dart              # 双栏编辑器
│   │       └── syntax_highlighter.dart        # Markdown 语法高亮
│   └── utils/
│       ├── file_utils.dart                    # 文件工具函数
│       └── platform_utils.dart                # 平台判断工具
├── test/
│   ├── core/
│   │   ├── config/
│   │   │   ├── app_config_test.dart
│   │   │   └── config_service_test.dart
│   │   └── theme/
│   │       └── app_theme_test.dart
│   ├── services/
│   │   ├── file_service_test.dart
│   │   └── markdown_parser_test.dart
│   ├── providers/
│   │   ├── settings_provider_test.dart
│   │   └── tab_provider_test.dart
│   └── ui/
│       └── editor/
│           └── syntax_highlighter_test.dart
├── LICENSE
└── README.md
docs/
├── i18n/
│   ├── README_zh-CN.md
│   ├── README_ja-JP.md
│   ├── README_ko-KR.md
│   ├── README_de-DE.md
│   ├── README_fr-FR.md
│   ├── README_it-IT.md
│   ├── README_ru-RU.md
│   ├── README_es-ES.md
│   ├── README_pt-PT.md
│   ├── README_ar-SA.md
│   └── README_pt-BR.md
└── v1.0.1/
    ├── PRD_需求文档.md
    ├── 详细设计文档.md
    ├── 设计规格文档.md
    └── 实施计划.md
```

---

## Task 1: Flutter 项目初始化

**Files:**
- Create: `code/pubspec.yaml`
- Create: `code/analysis_options.yaml`
- Create: `code/lib/main.dart`
- Create: `code/lib/core/constants.dart`

- [ ] **Step 1: 创建 Flutter 桌面项目**

```bash
cd /workspace/github-app/marktext-plus/code
flutter create --project-name marktext_plus --org com.marktextplus --platforms=windows,macos,linux .
```

- [ ] **Step 2: 配置 pubspec.yaml**

替换 `code/pubspec.yaml` 为：

```yaml
name: marktext_plus
description: A lightweight Markdown editor built with Flutter.
version: 1.0.1+1
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  flutter_riverpod: ^2.6.1
  window_manager: ^0.4.3
  file_picker: ^8.1.6
  path_provider: ^2.1.5
  url_launcher: ^6.3.1
  printing: ^5.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true
```

- [ ] **Step 3: 创建 analysis_options.yaml**

```yaml
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    prefer_const_declarations: true
    avoid_print: false
```

- [ ] **Step 4: 创建 constants.dart**

`code/lib/core/constants.dart`:

```dart
class AppConstants {
  AppConstants._();

  static const String appName = 'MarkText Plus';
  static const String appVersion = '1.0.1';
  static const String configFileName = 'config.json';
  static const String configDirName = 'marktext-plus';

  static const double defaultFontSize = 16.0;
  static const double defaultLineHeight = 1.6;
  static const double minFontSize = 12.0;
  static const double maxFontSize = 32.0;
  static const double defaultSplitRatio = 0.5;
  static const double minSplitRatio = 0.2;
  static const double maxSplitRatio = 0.8;

  static const int autoSaveDelay = 5000;
  static const int debounceDelay = 300;
  static const int maxRecentFiles = 20;

  static const double defaultWindowWidth = 1200;
  static const double defaultWindowHeight = 800;
  static const double minWindowWidth = 800;
  static const double minWindowHeight = 600;
}
```

- [ ] **Step 5: 创建最小 main.dart**

`code/lib/main.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

void main() {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const ProviderScope(child: MarkTextPlusApp()));
}

class MarkTextPlusApp extends StatelessWidget {
  const MarkTextPlusApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'MarkText Plus',
      theme: ThemeData.light(),
      home: const Scaffold(
        body: Center(child: Text('MarkText Plus V1.0.1')),
      ),
    );
  }
}
```

- [ ] **Step 6: 验证项目构建**

```bash
cd /workspace/github-app/marktext-plus/code && flutter pub get
flutter analyze
```

Expected: 无错误

- [ ] **Step 7: 提交**

```bash
git add code/
git commit -m "feat: initialize Flutter desktop project with core dependencies"
```

---

## Task 2: 配置持久化系统

**Files:**
- Create: `code/lib/core/config/app_config.dart`
- Create: `code/lib/core/config/config_service.dart`
- Create: `code/test/core/config/app_config_test.dart`
- Create: `code/test/core/config/config_service_test.dart`

- [ ] **Step 1: 编写 AppConfig 测试**

`code/test/core/config/app_config_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:marktext_plus/core/config/app_config.dart';

void main() {
  group('AppConfig', () {
    test('default values are correct', () {
      final config = AppConfig();
      expect(config.sideBarVisible, true);
      expect(config.tabBarVisible, true);
      expect(config.editMode, EditMode.preview);
      expect(config.splitRatio, 0.5);
      expect(config.fontSize, 16.0);
      expect(config.locale, 'en_US');
      expect(config.themeName, 'cadmiumLight');
      expect(config.autoSave, true);
    });

    test('toJson produces valid map', () {
      final config = AppConfig();
      final json = config.toJson();
      expect(json['sideBarVisible'], true);
      expect(json['editMode'], 'preview');
      expect(json['fontSize'], 16.0);
    });

    test('fromJson restores config correctly', () {
      final original = AppConfig(
        sideBarVisible: false,
        editMode: EditMode.split,
        fontSize: 20.0,
        locale: 'zh_CN',
      );
      final json = original.toJson();
      final restored = AppConfig.fromJson(json);
      expect(restored.sideBarVisible, false);
      expect(restored.editMode, EditMode.split);
      expect(restored.fontSize, 20.0);
      expect(restored.locale, 'zh_CN');
    });

    test('fromJson handles missing keys with defaults', () {
      final config = AppConfig.fromJson({'fontSize': 24.0});
      expect(config.fontSize, 24.0);
      expect(config.sideBarVisible, true);
      expect(config.editMode, EditMode.preview);
    });

    test('fromJson handles invalid data with defaults', () {
      final config = AppConfig.fromJson({'editMode': 'invalid', 'fontSize': 'bad'});
      expect(config.editMode, EditMode.preview);
      expect(config.fontSize, 16.0);
    });
  });
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd /workspace/github-app/marktext-plus/code && flutter test test/core/config/app_config_test.dart
```

Expected: FAIL — `app_config.dart` 不存在

- [ ] **Step 3: 实现 AppConfig**

`code/lib/core/config/app_config.dart`:

```dart
enum EditMode { source, preview, split }

class AppConfig {
  bool sideBarVisible;
  bool tabBarVisible;
  EditMode editMode;
  double splitRatio;
  String fontFamily;
  double fontSize;
  double lineHeight;
  bool autoSave;
  int autoSaveDelay;
  String themeName;
  bool darkMode;
  String locale;
  String bulletListMarker;
  int tabSize;
  bool enableHtml;
  double windowWidth;
  double windowHeight;
  double windowX;
  double windowY;
  bool isMaximized;
  List<String> recentFiles;

  AppConfig({
    this.sideBarVisible = true,
    this.tabBarVisible = true,
    this.editMode = EditMode.preview,
    this.splitRatio = 0.5,
    this.fontFamily = 'monospace',
    this.fontSize = 16.0,
    this.lineHeight = 1.6,
    this.autoSave = true,
    this.autoSaveDelay = 5000,
    this.themeName = 'cadmiumLight',
    this.darkMode = false,
    this.locale = 'en_US',
    this.bulletListMarker = '-',
    this.tabSize = 4,
    this.enableHtml = false,
    this.windowWidth = 1200,
    this.windowHeight = 800,
    this.windowX = 0,
    this.windowY = 0,
    this.isMaximized = false,
    this.recentFiles = const [],
  });

  Map<String, dynamic> toJson() => {
    'sideBarVisible': sideBarVisible,
    'tabBarVisible': tabBarVisible,
    'editMode': editMode.name,
    'splitRatio': splitRatio,
    'fontFamily': fontFamily,
    'fontSize': fontSize,
    'lineHeight': lineHeight,
    'autoSave': autoSave,
    'autoSaveDelay': autoSaveDelay,
    'themeName': themeName,
    'darkMode': darkMode,
    'locale': locale,
    'bulletListMarker': bulletListMarker,
    'tabSize': tabSize,
    'enableHtml': enableHtml,
    'windowWidth': windowWidth,
    'windowHeight': windowHeight,
    'windowX': windowX,
    'windowY': windowY,
    'isMaximized': isMaximized,
    'recentFiles': recentFiles,
  };

  factory AppConfig.fromJson(Map<String, dynamic> json) {
    return AppConfig(
      sideBarVisible: json['sideBarVisible'] as bool? ?? true,
      tabBarVisible: json['tabBarVisible'] as bool? ?? true,
      editMode: _parseEditMode(json['editMode']),
      splitRatio: (json['splitRatio'] as num?)?.toDouble() ?? 0.5,
      fontFamily: json['fontFamily'] as String? ?? 'monospace',
      fontSize: (json['fontSize'] as num?)?.toDouble() ?? 16.0,
      lineHeight: (json['lineHeight'] as num?)?.toDouble() ?? 1.6,
      autoSave: json['autoSave'] as bool? ?? true,
      autoSaveDelay: json['autoSaveDelay'] as int? ?? 5000,
      themeName: json['themeName'] as String? ?? 'cadmiumLight',
      darkMode: json['darkMode'] as bool? ?? false,
      locale: json['locale'] as String? ?? 'en_US',
      bulletListMarker: json['bulletListMarker'] as String? ?? '-',
      tabSize: json['tabSize'] as int? ?? 4,
      enableHtml: json['enableHtml'] as bool? ?? false,
      windowWidth: (json['windowWidth'] as num?)?.toDouble() ?? 1200,
      windowHeight: (json['windowHeight'] as num?)?.toDouble() ?? 800,
      windowX: (json['windowX'] as num?)?.toDouble() ?? 0,
      windowY: (json['windowY'] as num?)?.toDouble() ?? 0,
      isMaximized: json['isMaximized'] as bool? ?? false,
      recentFiles: (json['recentFiles'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList() ?? [],
    );
  }

  static EditMode _parseEditMode(dynamic value) {
    if (value is String) {
      return EditMode.values.where((e) => e.name == value).firstOrNull ?? EditMode.preview;
    }
    return EditMode.preview;
  }

  AppConfig copyWith({
    bool? sideBarVisible,
    bool? tabBarVisible,
    EditMode? editMode,
    double? splitRatio,
    String? fontFamily,
    double? fontSize,
    double? lineHeight,
    bool? autoSave,
    int? autoSaveDelay,
    String? themeName,
    bool? darkMode,
    String? locale,
    String? bulletListMarker,
    int? tabSize,
    bool? enableHtml,
    double? windowWidth,
    double? windowHeight,
    double? windowX,
    double? windowY,
    bool? isMaximized,
    List<String>? recentFiles,
  }) {
    return AppConfig(
      sideBarVisible: sideBarVisible ?? this.sideBarVisible,
      tabBarVisible: tabBarVisible ?? this.tabBarVisible,
      editMode: editMode ?? this.editMode,
      splitRatio: splitRatio ?? this.splitRatio,
      fontFamily: fontFamily ?? this.fontFamily,
      fontSize: fontSize ?? this.fontSize,
      lineHeight: lineHeight ?? this.lineHeight,
      autoSave: autoSave ?? this.autoSave,
      autoSaveDelay: autoSaveDelay ?? this.autoSaveDelay,
      themeName: themeName ?? this.themeName,
      darkMode: darkMode ?? this.darkMode,
      locale: locale ?? this.locale,
      bulletListMarker: bulletListMarker ?? this.bulletListMarker,
      tabSize: tabSize ?? this.tabSize,
      enableHtml: enableHtml ?? this.enableHtml,
      windowWidth: windowWidth ?? this.windowWidth,
      windowHeight: windowHeight ?? this.windowHeight,
      windowX: windowX ?? this.windowX,
      windowY: windowY ?? this.windowY,
      isMaximized: isMaximized ?? this.isMaximized,
      recentFiles: recentFiles ?? this.recentFiles,
    );
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd /workspace/github-app/marktext-plus/code && flutter test test/core/config/app_config_test.dart
```

Expected: ALL PASS

- [ ] **Step 5: 编写 ConfigService 测试**

`code/test/core/config/config_service_test.dart`:

```dart
import 'dart:io';
import 'package:flutter_test/flutter_test.dart';
import 'package:marktext_plus/core/config/app_config.dart';
import 'package:marktext_plus/core/config/config_service.dart';

void main() {
  late Directory tempDir;
  late ConfigService service;

  setUp(() {
    tempDir = Directory.systemTemp.createTempSync('config_test_');
    service = ConfigService(configDir: tempDir.path);
  });

  tearDown(() {
    tempDir.deleteSync(recursive: true);
  });

  group('ConfigService', () {
    test('load returns default config when file does not exist', () async {
      final config = await service.load();
      expect(config.fontSize, 16.0);
      expect(config.locale, 'en_US');
    });

    test('save and load round-trip preserves config', () async {
      final config = AppConfig(
        fontSize: 20.0,
        locale: 'zh_CN',
        sideBarVisible: false,
        editMode: EditMode.split,
      );
      await service.save(config);
      final loaded = await service.load();
      expect(loaded.fontSize, 20.0);
      expect(loaded.locale, 'zh_CN');
      expect(loaded.sideBarVisible, false);
      expect(loaded.editMode, EditMode.split);
    });

    test('load returns default config when file is corrupted', () async {
      final file = File('${tempDir.path}/config.json');
      file.writeAsStringSync('not valid json!!!');
      final config = await service.load();
      expect(config.fontSize, 16.0);
    });
  });
}
```

- [ ] **Step 6: 实现 ConfigService**

`code/lib/core/config/config_service.dart`:

```dart
import 'dart:convert';
import 'dart:io';
import 'package:path/path.dart' as p;
import 'app_config.dart';

class ConfigService {
  final String configDir;
  late final String _configPath;

  ConfigService({required this.configDir}) {
    _configPath = p.join(configDir, 'config.json');
  }

  Future<AppConfig> load() async {
    try {
      final file = File(_configPath);
      if (!await file.exists()) return AppConfig();
      final content = await file.readAsString();
      final json = jsonDecode(content) as Map<String, dynamic>;
      return AppConfig.fromJson(json);
    } catch (_) {
      return AppConfig();
    }
  }

  Future<void> save(AppConfig config) async {
    final file = File(_configPath);
    final dir = file.parent;
    if (!await dir.exists()) {
      await dir.create(recursive: true);
    }
    final content = const JsonEncoder.withIndent('  ').convert(config.toJson());
    await file.writeAsString(content);
  }
}
```

- [ ] **Step 7: 运行测试确认通过**

```bash
cd /workspace/github-app/marktext-plus/code && flutter test test/core/config/
```

Expected: ALL PASS

- [ ] **Step 8: 提交**

```bash
git add code/lib/core/config/ code/test/core/config/
git commit -m "feat: add config model and JSON persistence service with tests"
```

---

## Task 3: 主题系统

**Files:**
- Create: `code/lib/core/theme/app_theme.dart`
- Create: `code/test/core/theme/app_theme_test.dart`

- [ ] **Step 1: 编写主题测试**

`code/test/core/theme/app_theme_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:marktext_plus/core/theme/app_theme.dart';

void main() {
  group('AppTheme', () {
    test('has 5 built-in themes', () {
      expect(AppTheme.themes.length, 5);
    });

    test('cadmiumLight is a light theme', () {
      final theme = AppTheme.getTheme('cadmiumLight');
      expect(theme.brightness, Brightness.light);
    });

    test('oneDark is a dark theme', () {
      final theme = AppTheme.getTheme('oneDark');
      expect(theme.brightness, Brightness.dark);
    });

    test('unknown theme name returns cadmiumLight', () {
      final theme = AppTheme.getTheme('nonexistent');
      expect(theme.brightness, Brightness.light);
    });

    test('all theme names are available', () {
      expect(AppTheme.themeNames, containsAll([
        'cadmiumLight', 'oneDark', 'materialDark', 'graphiteLight', 'ulyssesLight',
      ]));
    });
  });
}
```

- [ ] **Step 2: 实现 AppTheme**

`code/lib/core/theme/app_theme.dart`:

```dart
import 'package:flutter/material.dart';

class AppTheme {
  AppTheme._();

  static const List<String> themeNames = [
    'cadmiumLight', 'oneDark', 'materialDark', 'graphiteLight', 'ulyssesLight',
  ];

  static final Map<String, ThemeData> themes = {
    'cadmiumLight': _buildTheme(
      brightness: Brightness.light,
      background: const Color(0xFFFFFFFF),
      foreground: const Color(0xFF333333),
      primary: const Color(0xFF4CAF50),
      surface: const Color(0xFFF5F5F5),
    ),
    'oneDark': _buildTheme(
      brightness: Brightness.dark,
      background: const Color(0xFF282C34),
      foreground: const Color(0xFFABB2BF),
      primary: const Color(0xFF61AFEF),
      surface: const Color(0xFF21252B),
    ),
    'materialDark': _buildTheme(
      brightness: Brightness.dark,
      background: const Color(0xFF212121),
      foreground: const Color(0xFFFFFFFF),
      primary: const Color(0xFF82B1FF),
      surface: const Color(0xFF303030),
    ),
    'graphiteLight': _buildTheme(
      brightness: Brightness.light,
      background: const Color(0xFFF5F5F5),
      foreground: const Color(0xFF333333),
      primary: const Color(0xFF607D8B),
      surface: const Color(0xFFEEEEEE),
    ),
    'ulyssesLight': _buildTheme(
      brightness: Brightness.light,
      background: const Color(0xFFFAFAFA),
      foreground: const Color(0xFF414141),
      primary: const Color(0xFF2196F3),
      surface: const Color(0xFFF0F0F0),
    ),
  };

  static ThemeData getTheme(String name) {
    return themes[name] ?? themes['cadmiumLight']!;
  }

  static ThemeData _buildTheme({
    required Brightness brightness,
    required Color background,
    required Color foreground,
    required Color primary,
    required Color surface,
  }) {
    return ThemeData(
      brightness: brightness,
      scaffoldBackgroundColor: background,
      colorScheme: ColorScheme(
        brightness: brightness,
        primary: primary,
        onPrimary: brightness == Brightness.dark ? Colors.black : Colors.white,
        secondary: primary,
        onSecondary: brightness == Brightness.dark ? Colors.black : Colors.white,
        error: Colors.red,
        onError: Colors.white,
        surface: surface,
        onSurface: foreground,
      ),
      textTheme: TextTheme(
        bodyMedium: TextStyle(color: foreground),
        bodyLarge: TextStyle(color: foreground),
      ),
      dividerColor: foreground.withValues(alpha: 0.12),
      iconTheme: IconThemeData(color: foreground.withValues(alpha: 0.7)),
    );
  }
}
```

- [ ] **Step 3: 运行测试确认通过**

```bash
cd /workspace/github-app/marktext-plus/code && flutter test test/core/theme/
```

Expected: ALL PASS

- [ ] **Step 4: 提交**

```bash
git add code/lib/core/theme/ code/test/core/theme/
git commit -m "feat: add theme system with 5 built-in themes"
```

---

## Task 4: 国际化框架

**Files:**
- Create: `code/lib/core/i18n/l10n/l10n.yaml`
- Create: `code/lib/core/i18n/l10n/app_en.arb` (模板)
- Create: 其余 11 个 `.arb` 翻译文件
- Create: `code/lib/providers/locale_provider.dart`

- [ ] **Step 1: 创建 l10n.yaml 配置**

`code/lib/core/i18n/l10n/l10n.yaml`（需复制到 `code/l10n.yaml`）:

```yaml
arb-dir: lib/core/i18n/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
```

- [ ] **Step 2: 创建英文模板 app_en.arb**

`code/lib/core/i18n/l10n/app_en.arb`:

```json
{
  "@@locale": "en",
  "appTitle": "MarkText Plus",
  "menuFile": "File",
  "menuEdit": "Edit",
  "menuView": "View",
  "menuFormat": "Format",
  "menuWindow": "Window",
  "menuHelp": "Help",
  "fileNew": "New File",
  "fileNewWindow": "New Window",
  "fileOpen": "Open File",
  "fileOpenFolder": "Open Folder",
  "fileSave": "Save",
  "fileSaveAs": "Save As",
  "fileExport": "Export",
  "fileExportHtml": "HTML",
  "fileExportPdf": "PDF",
  "fileSettings": "Settings",
  "fileQuit": "Quit",
  "editUndo": "Undo",
  "editRedo": "Redo",
  "editCut": "Cut",
  "editCopy": "Copy",
  "editPaste": "Paste",
  "editFind": "Find",
  "editReplace": "Replace",
  "editFindInFiles": "Find in Files",
  "viewEditMode": "Edit Mode",
  "viewSourceCode": "Source Code",
  "viewPreview": "Preview",
  "viewSplitView": "Split View",
  "viewShowSidebar": "Show Sidebar",
  "viewHideSidebar": "Hide Sidebar",
  "viewShowTabBar": "Show Tab Bar",
  "viewHideTabBar": "Hide Tab Bar",
  "viewFocusMode": "Focus Mode",
  "viewTypewriterMode": "Typewriter Mode",
  "viewZoomIn": "Zoom In",
  "viewZoomOut": "Zoom Out",
  "viewResetZoom": "Reset Zoom",
  "formatBold": "Bold",
  "formatItalic": "Italic",
  "formatStrikethrough": "Strikethrough",
  "formatHeading": "Heading {level}",
  "@formatHeading": { "placeholders": { "level": { "type": "int" } } },
  "formatOrderedList": "Ordered List",
  "formatUnorderedList": "Unordered List",
  "formatTaskList": "Task List",
  "formatCodeBlock": "Code Block",
  "formatQuoteBlock": "Quote Block",
  "formatMathBlock": "Math Block",
  "formatTable": "Table",
  "formatLink": "Link",
  "formatImage": "Image",
  "formatHorizontalRule": "Horizontal Rule",
  "windowMinimize": "Minimize",
  "windowFullScreen": "Toggle Full Screen",
  "windowAlwaysOnTop": "Always on Top",
  "helpAbout": "About MarkText Plus",
  "helpCheckUpdates": "Check for Updates",
  "helpChangelog": "Changelog",
  "helpReportBug": "Report Bug",
  "helpRequestFeature": "Request Feature",
  "helpGitHub": "GitHub Repository",
  "settingsGeneral": "General",
  "settingsEditor": "Editor",
  "settingsMarkdown": "Markdown",
  "settingsTheme": "Theme",
  "settingsKeybindings": "Keybindings",
  "settingsLanguage": "Language",
  "settingsAutoSave": "Auto Save",
  "settingsAutoSaveDelay": "Auto Save Delay (ms)",
  "settingsFontSize": "Font Size",
  "settingsLineHeight": "Line Height",
  "settingsTabSize": "Tab Size",
  "settingsEnableHtml": "Enable HTML",
  "settingsResetDefaults": "Reset to Defaults",
  "statusLine": "Ln {line}, Col {col}",
  "@statusLine": { "placeholders": { "line": { "type": "int" }, "col": { "type": "int" } } },
  "statusEncoding": "UTF-8",
  "statusMarkdown": "Markdown",
  "unsavedChanges": "Unsaved Changes",
  "unsavedChangesMessage": "Do you want to save changes before closing?",
  "save": "Save",
  "dontSave": "Don't Save",
  "cancel": "Cancel",
  "ok": "OK",
  "untitled": "Untitled",
  "openRecentFiles": "Open Recent Files",
  "noRecentFiles": "No Recent Files"
}
```

- [ ] **Step 3: 创建中文翻译 app_zh.arb**

`code/lib/core/i18n/l10n/app_zh.arb`:

```json
{
  "@@locale": "zh",
  "appTitle": "MarkText Plus",
  "menuFile": "文件",
  "menuEdit": "编辑",
  "menuView": "视图",
  "menuFormat": "格式",
  "menuWindow": "窗口",
  "menuHelp": "帮助",
  "fileNew": "新建文件",
  "fileNewWindow": "新建窗口",
  "fileOpen": "打开文件",
  "fileOpenFolder": "打开文件夹",
  "fileSave": "保存",
  "fileSaveAs": "另存为",
  "fileExport": "导出",
  "fileExportHtml": "HTML",
  "fileExportPdf": "PDF",
  "fileSettings": "设置",
  "fileQuit": "退出",
  "editUndo": "撤销",
  "editRedo": "重做",
  "editCut": "剪切",
  "editCopy": "复制",
  "editPaste": "粘贴",
  "editFind": "查找",
  "editReplace": "替换",
  "editFindInFiles": "在文件中查找",
  "viewEditMode": "编辑模式",
  "viewSourceCode": "源代码",
  "viewPreview": "预览",
  "viewSplitView": "双栏视图",
  "viewShowSidebar": "显示侧边栏",
  "viewHideSidebar": "隐藏侧边栏",
  "viewShowTabBar": "显示标签栏",
  "viewHideTabBar": "隐藏标签栏",
  "viewFocusMode": "专注模式",
  "viewTypewriterMode": "打字机模式",
  "viewZoomIn": "放大",
  "viewZoomOut": "缩小",
  "viewResetZoom": "重置缩放",
  "formatBold": "粗体",
  "formatItalic": "斜体",
  "formatStrikethrough": "删除线",
  "formatHeading": "{level} 级标题",
  "formatOrderedList": "有序列表",
  "formatUnorderedList": "无序列表",
  "formatTaskList": "任务列表",
  "formatCodeBlock": "代码块",
  "formatQuoteBlock": "引用块",
  "formatMathBlock": "数学公式",
  "formatTable": "表格",
  "formatLink": "链接",
  "formatImage": "图片",
  "formatHorizontalRule": "分隔线",
  "windowMinimize": "最小化",
  "windowFullScreen": "切换全屏",
  "windowAlwaysOnTop": "置顶",
  "helpAbout": "关于 MarkText Plus",
  "helpCheckUpdates": "检查更新",
  "helpChangelog": "更新日志",
  "helpReportBug": "报告问题",
  "helpRequestFeature": "功能建议",
  "helpGitHub": "GitHub 仓库",
  "settingsGeneral": "通用",
  "settingsEditor": "编辑器",
  "settingsMarkdown": "Markdown",
  "settingsTheme": "主题",
  "settingsKeybindings": "快捷键",
  "settingsLanguage": "语言",
  "settingsAutoSave": "自动保存",
  "settingsAutoSaveDelay": "自动保存延迟（毫秒）",
  "settingsFontSize": "字体大小",
  "settingsLineHeight": "行高",
  "settingsTabSize": "Tab 大小",
  "settingsEnableHtml": "启用 HTML",
  "settingsResetDefaults": "恢复默认设置",
  "statusLine": "行 {line}, 列 {col}",
  "statusEncoding": "UTF-8",
  "statusMarkdown": "Markdown",
  "unsavedChanges": "未保存的更改",
  "unsavedChangesMessage": "关闭前是否保存更改？",
  "save": "保存",
  "dontSave": "不保存",
  "cancel": "取消",
  "ok": "确定",
  "untitled": "未命名",
  "openRecentFiles": "最近打开的文件",
  "noRecentFiles": "没有最近的文件"
}
```

- [ ] **Step 4: 创建其余 10 个翻译文件**

按照 `app_en.arb` 的 key 结构，创建以下文件（翻译为对应语言）：
- `app_ja.arb` (日文)
- `app_ko.arb` (韩文)
- `app_de.arb` (德文)
- `app_fr.arb` (法文)
- `app_it.arb` (意大利文)
- `app_ru.arb` (俄文)
- `app_es.arb` (西班牙文)
- `app_pt.arb` (葡萄牙文)
- `app_ar.arb` (阿拉伯文)
- `app_pt_BR.arb` (巴西葡萄牙文)

每个文件格式与 `app_zh.arb` 相同，仅 `@@locale` 和翻译值不同。

- [ ] **Step 5: 创建 locale_provider.dart**

`code/lib/providers/locale_provider.dart`:

```dart
import 'dart:io';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../core/config/app_config.dart';

class LocaleNotifier extends StateNotifier<Locale> {
  LocaleNotifier(String savedLocale) : super(_parseLocale(savedLocale));

  static const supportedLocales = [
    Locale('en', 'US'),
    Locale('zh', 'CN'),
    Locale('ja', 'JP'),
    Locale('ko', 'KR'),
    Locale('de', 'DE'),
    Locale('fr', 'FR'),
    Locale('it', 'IT'),
    Locale('ru', 'RU'),
    Locale('es', 'ES'),
    Locale('pt', 'PT'),
    Locale('ar', 'SA'),
    Locale('pt', 'BR'),
  ];

  void setLocale(Locale locale) {
    state = locale;
  }

  bool get isRtl => state.languageCode == 'ar';

  static Locale _parseLocale(String code) {
    final parts = code.split('_');
    if (parts.length >= 2) {
      return Locale(parts[0], parts[1]);
    }
    return const Locale('en', 'US');
  }

  static Locale detectSystemLocale() {
    final systemLocale = Platform.localeName;
    final parsed = _parseLocale(systemLocale);
    for (final supported in supportedLocales) {
      if (supported.languageCode == parsed.languageCode) {
        return supported;
      }
    }
    return const Locale('en', 'US');
  }
}

final localeProvider = StateNotifierProvider<LocaleNotifier, Locale>((ref) {
  return LocaleNotifier('en_US');
});
```

- [ ] **Step 6: 运行 flutter gen-l10n 生成本地化代码**

```bash
cd /workspace/github-app/marktext-plus/code && flutter gen-l10n
```

Expected: 生成 `.dart_tool/flutter_gen/gen_l10n/` 下的本地化文件

- [ ] **Step 7: 提交**

```bash
git add code/lib/core/i18n/ code/lib/providers/locale_provider.dart code/l10n.yaml
git commit -m "feat: add i18n framework with 12 language translations"
```

---

## Task 5: 数据模型

**Files:**
- Create: `code/lib/models/file_node.dart`
- Create: `code/lib/models/tab_info.dart`

- [ ] **Step 1: 创建 FileNode 模型**

`code/lib/models/file_node.dart`:

```dart
class FileNode {
  final String name;
  final String path;
  final bool isDirectory;
  final List<FileNode> children;
  bool isExpanded;

  FileNode({
    required this.name,
    required this.path,
    this.isDirectory = false,
    this.children = const [],
    this.isExpanded = false,
  });

  String get extension => name.contains('.') ? name.split('.').last : '';
  bool get isMarkdown => const ['md', 'markdown', 'txt'].contains(extension.toLowerCase());
}
```

- [ ] **Step 2: 创建 TabInfo 模型**

`code/lib/models/tab_info.dart`:

```dart
import '../core/config/app_config.dart';

class TabInfo {
  final String id;
  final String? filePath;
  String fileName;
  String content;
  bool isModified;
  int cursorPosition;
  double scrollOffset;
  EditMode editMode;

  TabInfo({
    required this.id,
    this.filePath,
    this.fileName = 'Untitled',
    this.content = '',
    this.isModified = false,
    this.cursorPosition = 0,
    this.scrollOffset = 0,
    this.editMode = EditMode.preview,
  });

  TabInfo copyWith({
    String? fileName,
    String? content,
    bool? isModified,
    int? cursorPosition,
    double? scrollOffset,
    EditMode? editMode,
  }) {
    return TabInfo(
      id: id,
      filePath: filePath,
      fileName: fileName ?? this.fileName,
      content: content ?? this.content,
      isModified: isModified ?? this.isModified,
      cursorPosition: cursorPosition ?? this.cursorPosition,
      scrollOffset: scrollOffset ?? this.scrollOffset,
      editMode: editMode ?? this.editMode,
    );
  }
}
```

- [ ] **Step 3: 提交**

```bash
git add code/lib/models/
git commit -m "feat: add FileNode and TabInfo data models"
```

---

## Task 6: 工具类和文件服务

**Files:**
- Create: `code/lib/utils/platform_utils.dart`
- Create: `code/lib/utils/file_utils.dart`
- Create: `code/lib/services/file_service.dart`
- Create: `code/test/services/file_service_test.dart`

- [ ] **Step 1: 创建 platform_utils.dart**

```dart
import 'dart:io';

class PlatformUtils {
  PlatformUtils._();

  static bool get isWindows => Platform.isWindows;
  static bool get isMacOS => Platform.isMacOS;
  static bool get isLinux => Platform.isLinux;

  static String get modifierKey => isMacOS ? '⌘' : 'Ctrl';
  static String get altKey => isMacOS ? '⌥' : 'Alt';
}
```

- [ ] **Step 2: 创建 file_utils.dart**

```dart
import 'package:path/path.dart' as p;

class FileUtils {
  FileUtils._();

  static String getFileName(String path) => p.basename(path);

  static String getExtension(String path) => p.extension(path).toLowerCase();

  static bool isMarkdownFile(String path) {
    final ext = getExtension(path);
    return ['.md', '.markdown', '.txt'].contains(ext);
  }
}
```

- [ ] **Step 3: 编写 FileService 测试**

`code/test/services/file_service_test.dart`:

```dart
import 'dart:io';
import 'package:flutter_test/flutter_test.dart';
import 'package:marktext_plus/services/file_service.dart';

void main() {
  late Directory tempDir;
  late FileService service;

  setUp(() {
    tempDir = Directory.systemTemp.createTempSync('file_test_');
    service = FileService();
  });

  tearDown(() {
    tempDir.deleteSync(recursive: true);
  });

  group('FileService', () {
    test('readFile returns content of existing file', () async {
      final path = '${tempDir.path}/test.md';
      File(path).writeAsStringSync('# Hello');
      final content = await service.readFile(path);
      expect(content, '# Hello');
    });

    test('writeFile creates and writes file', () async {
      final path = '${tempDir.path}/output.md';
      await service.writeFile(path, '# World');
      expect(File(path).readAsStringSync(), '# World');
    });

    test('listDirectory returns file nodes', () async {
      File('${tempDir.path}/a.md').writeAsStringSync('');
      Directory('${tempDir.path}/sub').createSync();
      File('${tempDir.path}/sub/b.md').writeAsStringSync('');
      final nodes = await service.listDirectory(tempDir.path);
      expect(nodes.length, 2);
      expect(nodes.any((n) => n.name == 'a.md'), true);
      expect(nodes.any((n) => n.name == 'sub' && n.isDirectory), true);
    });
  });
}
```

- [ ] **Step 4: 实现 FileService**

`code/lib/services/file_service.dart`:

```dart
import 'dart:io';
import 'package:path/path.dart' as p;
import '../models/file_node.dart';

class FileService {
  Future<String> readFile(String path) async {
    return await File(path).readAsString();
  }

  Future<void> writeFile(String path, String content) async {
    await File(path).writeAsString(content);
  }

  Future<List<FileNode>> listDirectory(String dirPath) async {
    final dir = Directory(dirPath);
    final entities = await dir.list().toList();
    entities.sort((a, b) {
      final aIsDir = a is Directory;
      final bIsDir = b is Directory;
      if (aIsDir != bIsDir) return aIsDir ? -1 : 1;
      return p.basename(a.path).compareTo(p.basename(b.path));
    });
    return entities.map((e) {
      final name = p.basename(e.path);
      return FileNode(
        name: name,
        path: e.path,
        isDirectory: e is Directory,
      );
    }).toList();
  }

  Future<List<FileNode>> buildFileTree(String dirPath) async {
    final nodes = await listDirectory(dirPath);
    for (final node in nodes) {
      if (node.isDirectory) {
        final children = await buildFileTree(node.path);
        nodes[nodes.indexOf(node)] = FileNode(
          name: node.name,
          path: node.path,
          isDirectory: true,
          children: children,
        );
      }
    }
    return nodes;
  }
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
cd /workspace/github-app/marktext-plus/code && flutter test test/services/
```

Expected: ALL PASS

- [ ] **Step 6: 提交**

```bash
git add code/lib/utils/ code/lib/services/file_service.dart code/test/services/
git commit -m "feat: add file service, platform utils, and file utils"
```

---

## Task 7: Markdown 解析器

**Files:**
- Create: `code/lib/services/markdown_parser.dart`
- Create: `code/test/services/markdown_parser_test.dart`

- [ ] **Step 1: 编写 Markdown 解析器测试**

`code/test/services/markdown_parser_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:marktext_plus/services/markdown_parser.dart';

void main() {
  late MarkdownParser parser;
  setUp(() => parser = MarkdownParser());

  group('Block parsing', () {
    test('parses heading', () {
      final nodes = parser.parse('# Hello');
      expect(nodes.first.type, NodeType.heading);
      expect((nodes.first as HeadingNode).level, 1);
    });

    test('parses paragraph', () {
      final nodes = parser.parse('Hello world');
      expect(nodes.first.type, NodeType.paragraph);
    });

    test('parses unordered list', () {
      final nodes = parser.parse('- item1\n- item2');
      expect(nodes.first.type, NodeType.unorderedList);
    });

    test('parses code block', () {
      final nodes = parser.parse('```dart\nprint("hi");\n```');
      expect(nodes.first.type, NodeType.codeBlock);
    });

    test('parses blockquote', () {
      final nodes = parser.parse('> quote text');
      expect(nodes.first.type, NodeType.blockquote);
    });

    test('parses horizontal rule', () {
      final nodes = parser.parse('---');
      expect(nodes.first.type, NodeType.horizontalRule);
    });
  });

  group('Inline parsing', () {
    test('parses bold', () {
      final spans = parser.parseInline('**bold**');
      expect(spans.any((s) => s.type == InlineType.bold), true);
    });

    test('parses italic', () {
      final spans = parser.parseInline('*italic*');
      expect(spans.any((s) => s.type == InlineType.italic), true);
    });

    test('parses inline code', () {
      final spans = parser.parseInline('`code`');
      expect(spans.any((s) => s.type == InlineType.code), true);
    });

    test('parses link', () {
      final spans = parser.parseInline('[text](url)');
      expect(spans.any((s) => s.type == InlineType.link), true);
    });

    test('parses image', () {
      final spans = parser.parseInline('![alt](url)');
      expect(spans.any((s) => s.type == InlineType.image), true);
    });
  });
}
```

- [ ] **Step 2: 实现 MarkdownParser**

`code/lib/services/markdown_parser.dart` — 自研轻量解析器，包含：
- `MarkdownNode` 抽象基类 + 具体节点类型（HeadingNode, ParagraphNode, CodeBlockNode, ListNode, BlockquoteNode, HorizontalRuleNode, TableNode）
- `InlineSpan` 类型（bold, italic, code, link, image, strikethrough, text）
- `NodeType` 和 `InlineType` 枚举
- `parse(String markdown)` → `List<MarkdownNode>` 块级解析
- `parseInline(String text)` → `List<InlineSpan>` 行内解析
- 使用正则匹配，逐行扫描，支持 CommonMark + GFM 子集

- [ ] **Step 3: 运行测试确认通过**

```bash
cd /workspace/github-app/marktext-plus/code && flutter test test/services/markdown_parser_test.dart
```

- [ ] **Step 4: 提交**

```bash
git add code/lib/services/markdown_parser.dart code/test/services/markdown_parser_test.dart
git commit -m "feat: add self-built Markdown parser with block and inline parsing"
```

---

## Task 8: 语法高亮器

**Files:**
- Create: `code/lib/ui/editor/syntax_highlighter.dart`
- Create: `code/test/ui/editor/syntax_highlighter_test.dart`

- [ ] **Step 1: 编写语法高亮测试**

测试 `MarkdownSyntaxHighlighter.highlight(text)` 返回正确的 `TextSpan` 树：
- `# heading` → 标题颜色
- `**bold**` → 粗体样式
- `` `code` `` → 代码颜色
- `[link](url)` → 链接颜色

- [ ] **Step 2: 实现 MarkdownSyntaxHighlighter**

基于正则匹配，将 Markdown 源文本转换为带样式的 `TextSpan` 列表。颜色从当前主题获取。

- [ ] **Step 3: 运行测试，提交**

```bash
git commit -m "feat: add Markdown syntax highlighter for source editor"
```

---

## Task 9: Riverpod Providers

**Files:**
- Create: `code/lib/providers/settings_provider.dart`
- Create: `code/lib/providers/editor_provider.dart`
- Create: `code/lib/providers/tab_provider.dart`
- Create: `code/lib/providers/file_provider.dart`

- [ ] **Step 1: 实现 SettingsProvider**

`settings_provider.dart` — `StateNotifier<AppConfig>`，方法：
- `updateConfig(AppConfig Function(AppConfig) updater)` — 更新配置并触发 `ConfigService.save()`
- `resetDefaults()` — 恢复默认配置
- `toggleSideBar()`, `toggleTabBar()`, `setEditMode(EditMode)`, `setLocale(String)`, `setTheme(String)` 等快捷方法

- [ ] **Step 2: 实现 TabProvider**

`tab_provider.dart` — `StateNotifier<List<TabInfo>>`，方法：
- `addTab(TabInfo)`, `removeTab(String id)`, `setActiveTab(String id)`
- `updateContent(String id, String content)`, `markSaved(String id)`
- `activeTab` getter

- [ ] **Step 3: 实现 EditorProvider**

`editor_provider.dart` — 管理当前编辑器状态：
- 光标位置、选中范围、滚动偏移
- `TextEditingController` 生命周期

- [ ] **Step 4: 实现 FileProvider**

`file_provider.dart` — `StateNotifier<List<FileNode>>`，方法：
- `loadDirectory(String path)` — 加载文件树
- `toggleExpand(String path)` — 展开/折叠目录

- [ ] **Step 5: 编写 settings_provider 和 tab_provider 测试**

- [ ] **Step 6: 运行测试，提交**

```bash
git commit -m "feat: add Riverpod providers for settings, tabs, editor, and files"
```

---

## Task 10: UI 外壳组件

**Files:**
- Create: `code/lib/ui/widgets/app_menu_bar.dart`
- Create: `code/lib/ui/widgets/side_bar.dart`
- Create: `code/lib/ui/widgets/editor_tab_bar.dart`
- Create: `code/lib/ui/widgets/status_bar.dart`
- Create: `code/lib/ui/screens/home_screen.dart`

- [ ] **Step 1: 实现 AppMenuBar**

使用 Flutter `MenuBar` Widget，菜单结构按设计规格文档 Section 7 实现。所有文本使用 `AppLocalizations` 国际化。快捷键按平台区分（Ctrl/Cmd）。

- [ ] **Step 2: 实现 SideBar**

左侧图标列（文件、搜索、TOC、设置）+ 右侧内容区（文件树/搜索结果/TOC）。可通过 `settingsProvider.toggleSideBar()` 显示/隐藏。

- [ ] **Step 3: 实现 EditorTabBar**

标签栏：显示打开的文件标签，支持点击切换、中键关闭、拖拽排序。未保存文件显示圆点标记。

- [ ] **Step 4: 实现 StatusBar**

底部状态栏：行号/列号、编码、文件类型、换行符、缩放比例。

- [ ] **Step 5: 实现 HomeScreen**

组合所有组件的主页面布局：
```
Column([
  AppMenuBar,
  Expanded(Row([
    if (sideBarVisible) SideBar,
    Expanded(Column([
      if (tabBarVisible) EditorTabBar,
      Expanded(EditorArea),  // 根据 editMode 切换
    ])),
  ])),
  StatusBar,
])
```

- [ ] **Step 6: 提交**

```bash
git commit -m "feat: add main UI shell with menu bar, sidebar, tab bar, and status bar"
```

---

## Task 11: 三种编辑器模式

**Files:**
- Create: `code/lib/ui/editor/source_editor.dart`
- Create: `code/lib/ui/editor/markdown_renderer.dart`
- Create: `code/lib/ui/editor/split_editor.dart`

- [ ] **Step 1: 实现 SourceEditor**

基于 `TextField` + `TextEditingController`：
- 左侧行号栏（`CustomPaint` 或 `Row`）
- `MarkdownSyntaxHighlighter` 提供语法高亮 `TextSpan`
- 支持 Tab 键插入空格（根据 `tabSize` 配置）
- 内容变更通过 `tabProvider.updateContent()` 同步

- [ ] **Step 2: 实现 MarkdownRenderer**

将 `MarkdownParser.parse()` 的 AST 转换为 Flutter Widget 树：
- `HeadingNode` → 不同大小的 `Text`
- `ParagraphNode` → `RichText`（含行内样式）
- `CodeBlockNode` → 带背景色的 `Container` + 等宽字体 `Text`
- `ListNode` → `Column` + 缩进 + 标记符号
- `BlockquoteNode` → 左侧竖线 + 缩进内容
- `TableNode` → `Table` Widget
- `HorizontalRuleNode` → `Divider`
- 图片 → `Image.file` 或 `Image.network`
- 链接 → `GestureDetector` + `url_launcher`

- [ ] **Step 3: 实现 SplitEditor**

```dart
Row([
  Expanded(flex: leftFlex, child: SourceEditor(...)),
  GestureDetector(onHorizontalDragUpdate: ..., child: divider),
  Expanded(flex: rightFlex, child: MarkdownRenderer(..., readOnly: true)),
])
```

- 分隔条拖动更新 `splitRatio`，持久化到配置
- 左侧编辑触发 300ms 防抖 → 右侧重新渲染
- 滚动同步：监听左侧 `ScrollController`，按比例映射到右侧

- [ ] **Step 4: 在 HomeScreen 中集成模式切换**

根据 `settingsProvider.editMode` 切换显示：
- `EditMode.source` → `SourceEditor`
- `EditMode.preview` → `MarkdownRenderer`（可编辑）
- `EditMode.split` → `SplitEditor`

- [ ] **Step 5: 提交**

```bash
git commit -m "feat: implement three editor modes - source, preview, and split view"
```

---

## Task 12: App 入口和窗口管理

**Files:**
- Modify: `code/lib/main.dart`
- Create: `code/lib/app.dart`

- [ ] **Step 1: 实现 app.dart**

`MaterialApp` 配置：
- `theme` / `darkTheme` 绑定 `settingsProvider`
- `locale` 绑定 `localeProvider`
- `localizationsDelegates` 和 `supportedLocales` 配置
- `Directionality` 包裹（RTL 支持）
- `home` 指向 `HomeScreen`

- [ ] **Step 2: 更新 main.dart**

启动流程：
1. `WidgetsFlutterBinding.ensureInitialized()`
2. `await windowManager.ensureInitialized()` — 设置窗口大小/位置
3. `ConfigService.load()` — 加载配置
4. `runApp(ProviderScope(overrides: [...], child: MarkTextPlusApp()))`

- [ ] **Step 3: 提交**

```bash
git commit -m "feat: wire up app entry with window manager, config loading, and i18n"
```

---

## Task 13: 设置页面

**Files:**
- Create: `code/lib/ui/screens/settings_screen.dart`

- [ ] **Step 1: 实现设置页面**

左侧分类导航（General/Editor/Markdown/Theme/Keybindings），右侧对应设置项。所有设置项通过 `settingsProvider` 读写，变更立即持久化。

关键设置项：
- 语言选择下拉框（12 种语言）
- 主题选择（5 种主题 + 跟随系统）
- 编辑模式默认值
- 字体大小滑块
- 自动保存开关
- Tab 大小
- 恢复默认按钮

- [ ] **Step 2: 提交**

```bash
git commit -m "feat: add settings screen with all configuration options"
```

---

## Task 14: LICENSE 和 README

**Files:**
- Create: `code/LICENSE`
- Create: `code/README.md`
- Create: `docs/i18n/README_zh-CN.md` (及其余 10 个语言)

- [ ] **Step 1: 创建 LICENSE**

MIT License，包含原 MarkText 版权声明和本项目版权声明。

- [ ] **Step 2: 创建英文 README.md**

顶部语言切换链接，内容包含：项目介绍、功能特性、截图占位、安装方式、开发指南、协议声明。

- [ ] **Step 3: 创建 11 个语言的 README 翻译**

放在 `docs/i18n/` 下，文件名格式 `README_xx-XX.md`。

- [ ] **Step 4: 提交**

```bash
git commit -m "feat: add LICENSE and multi-language README files"
```

---

## Task 15: 集成验证

- [ ] **Step 1: 运行全部测试**

```bash
cd /workspace/github-app/marktext-plus/code && flutter test
```

Expected: ALL PASS

- [ ] **Step 2: 静态分析**

```bash
flutter analyze
```

Expected: 无错误

- [ ] **Step 3: 构建验证**

```bash
flutter build linux --release
```

Expected: 构建成功

- [ ] **Step 4: 最终提交**

```bash
git add -A
git commit -m "chore: integration verification - all tests pass, build succeeds"
```

---

**计划结束**


